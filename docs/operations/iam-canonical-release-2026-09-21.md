# Canonical IAM compatibility deployment — 2026-09-21

Browser's native backend is deployed from commit `7c18cde867e1736d9df99655b2ca3faaacfe2cff`, with IAM SDK 3.0.1. Public Browser packages and API remain compatible at 0.2.4. The deployed ARM64 executable is `c41a645a5fd0` (SHA-256 prefix), release directory `20260921-7c18cde-c41a645a5fd0`. The final immutable artifact and complete hash are retained in the private service release bucket.

Validation passed: 221 backend tests, workspace tests and warning-denied Clippy; GitHub CI run 35584122293 passed its Rust, frontend, installer and documentation checks. Before deployment, the offline converter rehearsed production and ten registered testing databases. It checked integrity, converted their separate copies with the existing encryption key, and compared every retained non-identity ledger column. A stopped-service snapshot and a second rehearsal preceded conversion of all eleven live databases. The original runtime configuration/key and service unit were retained. Complete quiesced snapshots and a checksum/row-count manifest are retained both on the host and under the private bucket's `backups/canonical-cutover-34bebc1/` prefix.

The initial candidate could not execute because its staging directory lacked traversal permission for the service user. Systemd rejected execution before any process or worker ran. Correcting that directory to 0755 restored startup without rolling back data. An authenticated smoke then found the older IAM unscoped response needed canonical identity extraction from `authorizations[]` as well as `authorization`; the final commit adds that strict compatibility path and its regression.

The final service and backup timer are active. Public health returned 200, and anonymous `/api/v1/me` returned 401. Forty authenticated checks passed against the still-deployed pre-canonical IAM: repeat actor login, identity, organization discovery, services/profiles/sessions/recordings/usage, refresh, cross-environment rejection and rejection of test credentials on production routes. No provider browser sessions were created. A separate Browser session was retained for the post-IAM-cutover credential/replay check.

Existing saved test application credentials were already rejected before this deployment. Verification used a new bounded environment and direct IAM test signup/import. A separate Honeycomb import attempt reported missing Browser protected lifecycle transport; its partial environment and operation were preserved for separate investigation. This deployment does not claim that integration is configured.

Use the [cutover procedure](iam-canonical-identities.md) for rollback limits. After new workers have run, retain their current data and reconcile external work before considering restoration of an older snapshot.
