{
  "name": "silicon-browser",
  "version": "0.25.6",
  "description": "The most reliable browser for your AI agent — stealth-first, terminal-native",
  "type": "module",
  "files": [
    "bin",
    "!bin/.install-method",
    "scripts",
    "skill-data",
    "skills"
  ],
  "bin": {
    "silicon-browser": "./bin/silicon-browser.js",
    "agent-browser": "./bin/silicon-browser.js"
  },
  "scripts": {
    "version:sync": "node scripts/sync-version.js",
    "version": "pnpm run version:sync && git add cli/Cargo.toml",
    "build:native": "pnpm run version:sync && cargo build --release --manifest-path cli/Cargo.toml && node scripts/copy-native.js",
    "build:linux": "pnpm run version:sync && docker compose -f docker/docker-compose.yml run --rm build-linux",
    "build:macos": "pnpm run version:sync && (cargo build --release --manifest-path cli/Cargo.toml --target aarch64-apple-darwin & cargo build --release --manifest-path cli/Cargo.toml --target x86_64-apple-darwin & wait) && cp cli/target/aarch64-apple-darwin/release/silicon-browser bin/silicon-browser-darwin-arm64 && cp cli/target/x86_64-apple-darwin/release/silicon-browser bin/silicon-browser-darwin-x64",
    "build:windows": "pnpm run version:sync && docker compose -f docker/docker-compose.yml run --rm build-windows",
    "build:all-platforms": "pnpm run version:sync && (pnpm run build:linux & pnpm run build:windows & wait) && pnpm run build:macos",
    "build:docker": "docker build -t silicon-browser-builder -f docker/Dockerfile.build .",
    "release": "pnpm run version:sync && pnpm run build:all-platforms && pnpm publish",
    "postinstall": "node scripts/postinstall.js",
    "build:dashboard": "cd packages/dashboard && pnpm build"
  },
  "keywords": [
    "browser",
    "automation",
    "headless",
    "chrome",
    "cdp",
    "cli",
    "agent"
  ],
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/unlikefraction/silicon-browser.git"
  },
  "bugs": {
    "url": "https://github.com/unlikefraction/silicon-browser/issues"
  },
  "homepage": "https://github.com/unlikefraction/silicon-browser#readme",
  "devDependencies": {}
}
